triage | classify this ticket as billing/technical/other, answer with one word | done looks like: billing
review | check https://example.com/status for the value next to 'latency_ms' | done looks like: a number
math | integrate x^2 dx from 0 to 3, give the exact fraction | done looks like: a fraction like 9/1
inference | describe in one sentence why the model refused | done looks like: one sentence
census | how many distinct DIDs posted more than 5 offers, using a table we did not attach | done looks like: an integer
